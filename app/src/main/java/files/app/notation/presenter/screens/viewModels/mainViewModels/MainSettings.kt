package files.app.notation.presenter.screens.viewModels.mainViewModels

enum class MainSettings(val nameValue:String) {
    IN_CELLS("In cells"),
    LINE_BY_LINE("Line by line"),
    SORT_BY_DATE("Sort by date"),
    SORT_BY_TITLE("Sort by title")
}